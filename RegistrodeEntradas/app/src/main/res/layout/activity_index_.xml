<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".index_Activity">

    <TextView
        android:id="@+id/txt_index"
        android:layout_width="285dp"
        android:layout_height="36dp"
        android:layout_marginEnd="60dp"
        android:text="@string/txt_index"
        android:textAlignment="center"
        app:autoSizeTextType="uniform"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintVertical_bias="0.155" />

    <ImageButton
        android:id="@+id/on"
        android:layout_width="47dp"
        android:layout_height="50dp"
        android:layout_marginStart="132dp"
        android:layout_marginTop="52dp"
        android:contentDescription="@string/imb_enter"
        android:onClick="Imput"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@+id/txt_index"
        app:srcCompat="@mipmap/ic_launcher_inv_enter" />

    <ImageButton
        android:id="@+id/imb_stock"
        android:layout_width="50dp"
        android:layout_height="47dp"
        android:layout_marginStart="64dp"
        android:layout_marginTop="200dp"
        app:layout_constraintStart_toEndOf="@+id/on"
        app:layout_constraintTop_toTopOf="parent"
        app:srcCompat="@mipmap/ic_launcher_stock" />

</androidx.constraintlayout.widget.ConstraintLayout>